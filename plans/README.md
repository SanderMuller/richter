# Implementation Plans

Generated by the improve skill on 2026-07-16, from the audit in
[audit-2026-07-16.md](audit-2026-07-16.md) (pinned at commit `50a0efa`).
**Second round added 2026-07-19** (plans 017–030) from
[audit-2026-07-19.md](audit-2026-07-19.md) (pinned at commit `822a3c8`).
**Handoff round added 2026-07-19** (plans 031–032, pinned at commit
`0c2e5d1`) from a consumer detection-gaps handoff — see the "Post-audit
handoff" section of [audit-2026-07-19.md](audit-2026-07-19.md).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

> **Integrated (2026-07-17)**: plans 001-006, 010, 011 (minus its binding
> half) and 012 were cherry-picked onto main via `advisor/integration` and
> fast-forward merged at `db3d192`, closing with a green `composer qa-check`
> (334 tests). The per-plan branches and agent worktrees are now redundant.
>
> **Working-tree note (2026-07-16)**: plans 002-010 were written against a
> working tree carrying substantial uncommitted changes on top of `50a0efa`
> (GraphCache, MarkdownFormatter, --explain, and related test/config work).
> Their "Current state" excerpts reflect that working tree. Each plan's drift
> check therefore compares excerpts against live code rather than relying on
> a commit-range diff.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Stop the diff parser from misreading hunk-body lines starting `++ `/`-- ` as file headers | P1 | S | — | DONE (reviewed; branch `advisor/001-diff-parser-hunk-state`, commit `a5dd2c4`) |
| 002  | Make the MCP detect-changes tool return a clean error for an option-shaped base ref | P1 | S | — | DONE (reviewed; branch `advisor/002-mcp-detect-changes-catch`, commit `47694b4`) |
| 003  | Correct the README's "Nothing runs" safety claim | P1 | S | — | DONE (reviewed; branch `advisor/003-readme-nothing-runs-claim`, commit `7bed363`) |
| 004  | Restore the laravel-brain config keys after a graph build | P2 | S | — | DONE (reviewed; branch `advisor/004-brain-config-restore`, commit `4287e64`; rector flags 2 pre-existing WIP findings, not from this plan) |
| 005  | Route all name resolution through the AppFiles helpers | P2 | S | — | DONE (reviewed; branch `advisor/005-dedupe-name-resolution`, commit `c58ddca`) |
| 006  | Exercise the benchmark's pass path end-to-end | P2 | M | — | DONE (reviewed; branch `advisor/006-benchmark-pass-path-coverage`, commit `2ac98ed`; plan premise corrected: skeleton app/ is empty, tests point base_path at the fixture project) |
| 007  | Replace the per-seed full-graph regex scan with a token index | P2 | M | — | DONE (reviewed; cherry-picked to main as `ff6f886`, 2026-07-18) |
| 008  | Collect AST nodes once per file for the tracers | P3 | M | 005 | DONE (reviewed; commit `0f66683`; merged to main on 2026-07-18) |
| 009  | Reuse the consolidated pass's ASTs for the entry-point tracer | P3 | L | 005, 008 | DONE (reviewed; commit `e98e369`; merged to main with 008 on 2026-07-18) |
| 010  | Investigate/fix the eager-load model-methods cache staleness | P3 | S | — | DONE (reviewed; staleness confirmed by test, fixed; branch `advisor/010-eager-load-cache-staleness`, commit `bcab10c`) |
| 011  | Cover the interface/binding edges and MCP success paths | P2 | M | — | DONE (reviewed; binding half removed from scope — blocked on finding 19, upstream Brain bug; branch `advisor/011-interface-binding-mcp-coverage`, commits `9d7a43d`+`8bb2535`) |
| 012  | Replace the Brain container-binding scan with a Richter-native one (finding 19 workaround) | P1 | M | 005 | DONE (reviewed; branch `advisor/012-native-binding-edges` off 005's tip, commit `6c3ed4c`; upstream fix PR'd as laramint/laravel-brain#65) |
| 013  | Make pure renames visible — a moved class must never read as "no impact" (finding 4) | P1 | M | — | DONE (reviewed + 22-agent adversarial verification, 0 confirmed findings; commit `53a5121`, merged to main 2026-07-18) |
| 015  | Emit structured MCP output — prose plus the documented --json contract (D3) | P2 | S | — | DONE (reviewed; cherry-picked to main as `1ac82de`, 2026-07-18) |
| 016  | Add richter:benchmark:add — scaffold a benchmark fixture from a fix commit (D4) | P3 | M | — | DONE (reviewed; cherry-picked to main as `d69e15b`, 2026-07-18) |
| 017  | Bound the laravel/mcp compatibility range and test the mcp-absent path in CI (F1) | P1 | S | — | DONE (reviewed; main `1cb683c`, 2026-07-19; 0.9.0 validated compatible; no-mcp CI job removes boost too — see commit body) |
| 018  | Resolve Blade-view seeds by exact node membership, not boundary substring (F2) | P1 | S | 020 preferred first | DONE (reviewed; main `e616a55`, 2026-07-19; directSeed loop extracted to directWalkSeeds() to hold the complexity limit) |
| 019  | Close the frontend lane's fail-toward-under-reporting gaps (F3, F4, F11) | P1 | M | 020 preferred first | DONE (reviewed; main `962ad33`+`2596859`+`85a1374`+`1a3f2ad`, 2026-07-19; four fixes test-first) |
| 020  | Cover the frontend/Blade-inline seam of ChangedSymbols::resolve() end-to-end (F5) | P1 | M | — | DONE (reviewed; main `46fae5f`, 2026-07-19; fixtures aligned to real routes/web.php, standalone view pins honest UNRESOLVED) |
| 021  | Handle git's quoted pathnames so non-ASCII files never read as "no impact" (F6) | P2 | M | — | DONE (reviewed; main `7d24245`, 2026-07-19) |
| 022  | Extract the duplicated line-range scoping shared by the source checkers (F7) | P3 | S | — | DONE (reviewed; main `5c84800`, 2026-07-19) |
| 023  | Pin benchmark:add's config-stanza escaping with tests (F8) | P3 | S | — | DONE (reviewed; main `24a7d90`, 2026-07-19; eval round-trip replaced by exact-line pins per the plan's ruleset escape hatch) |
| 024  | Escape file paths in the markdown formatter's structural positions (F9) | P2 | S | — | DONE (reviewed; main `5f5fb43`, 2026-07-19) |
| 025  | One report fixture across all three formatters, then de-duplicate the row building (F10) | P2 | M | 024 | DONE (reviewed; main `a55f5eb`+`82874aa`, 2026-07-19; EntryPointRow owns facts+ordering, zero golden-assertion changes; ordering now plain-label in both formatters — cosmetic edge-case difference vs old decorated sort, deliberate) |
| 026  | Memoize riskInputs' repeated graph walks within one detectChanges run (F12) | P3 | M | — | DONE (reviewed; main `b2019b0`, 2026-07-19; run-scoped memo by reference — instance property impossible on a readonly class, the plan's documented fallback) |
| 027  | Cache Composer downloads in the run-tests matrix (F13) | P3 | S | 017 soft (reconcile jobs) | DONE (reviewed; main `146b6a6`, 2026-07-19; both jobs cached, -nomcp key suffix) |
| 028  | Align the v0.7.0 CHANGELOG entry with the release notes (F14) | P3 | S | — | DONE (reviewed; main `f19fd14`, 2026-07-19; published release body verified identical to notes file — no maintainer reconciliation needed) |
| 029  | Spike: a real benchmark corpus and a CI replay (DIR-1) | P2 | L | — | DONE (design doc at internal/spike-benchmark-corpus.md; option B confirmed infeasible — git pathspecs resolve against repo root; recommendation: host-app corpus, CI YAML copy-ready in the doc) |
| 030  | Pin the Livewire/Filament test-selection idioms and close the string-name gap (DIR-2) | P2 | M | — | DONE (reviewed; main `c3d7735`+`41b1a23`, 2026-07-19; relative-qualification miss pinned as characterization) |
| 031  | Anchor literal-URI candidates to call arguments; extend generated-path exclusion to files/globs, Ziggy defaults, and `.d.ts` (handoff H1+H2) | P2 | M | 019 | DONE (reviewed; main `1d04678`+`23d90ac`+`b398c51`+`fc7dd3d`+`fb4845d`, 2026-07-19; supersedes the spike doc over-matching acceptance, recall losses documented in the scanner docblock) |
| 032  | Resolve same-module constant `route()` arguments before tainting a file UNRESOLVED (handoff H3) | P3 | M | 019; 031 soft | DONE (reviewed; main `b2ece91`+`9b221c8`, 2026-07-19; no-guess const/enum resolution, taint preserved on any miss) |

| 033  | Tag test-referenced entry points whose referencing tests prove nothing (tier-3 handoff #2) | P2 | M | — | DONE (reviewed; main, 5 commits ending at the Rector-conventions fixup, 2026-07-20; per-file certainty-gated grading, entryPointTestReferences in JSON+MCP) |

| 034  | Add --profile to detect-changes: phase timings for the incremental-rebuild decision (tier-3 handoff #1, measurement gate) | P3 | S–M | 033 (same command file) | TODO |

| 035  | Draft the upstream laravel-brain incremental/provenance issue + profile decision procedure (tier-3 handoff #1 follow-through) | P3 | S | 034 soft (issue reads better once --profile exists) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 001 folds in the diff-parser characterization tests from audit finding 11
  (CRLF, binary, mode-only) so current behavior is pinned before the
  header-state change.
- A future plan for audit finding 4 (pure renames invisible to the parser)
  touches the same parse loop and must land **after** 001 — its rename-header
  branch belongs with the `! $inHunk`-guarded header branches 001 introduces.
- 006 gains value after 001 (the replay pipeline then guards real parser
  fixes) but does not depend on it mechanically.
- 008 requires 005 (it builds on the consolidated name-resolution helpers).
- 009 requires 005 and 008 (it rebases on the split tracer methods and the
  reordered build).
- 004 and the GraphCache fingerprint interact — plan 004 explains why the
  fingerprint logic stays untouched and only its comment updates.

Second round (2026-07-19):

- **020 before 018/019 preferred, not required**: 020's seam tests pin current
  frontend behavior; 018/019 then change that behavior knowingly (their plans
  say what may need a deliberate expectation update). Reverse order works but
  loses the characterization value.
- 025 requires 024 (both edit `MarkdownFormatter`; 024 is small — land it
  first, and 025's shared rows must keep 024's `pathCell` escaping in the
  markdown layer).
- 027 has a soft interaction with 017: if 017's `test-without-mcp` job exists,
  027 adds the cache step there too (plan 027 step 2 handles both cases).
- 018 and 026 both edit `ImpactAnalyzer.php` in different methods — execute
  sequentially, expect at most trivial adjacent-hunk merges.
- 029 (benchmark corpus spike) gates the *unplanned* direction items DIR-3
  (configurable risk-floor namespaces) and the handoff's entry-point widening —
  neither may be planned as a code change until the corpus exists.

Handoff round (2026-07-19):

- **031/032 touch the same files as 019** (`FrontendReferenceScanner.php`,
  `FrontendChanges.php`, the two frontend test files) — execute after 019
  integrates. Their "Current state" excerpts are from pre-019 code at
  `0c2e5d1`; each plan carries an "Expected drift" section listing exactly
  what 019 changes, so reconcile against it instead of STOPping on the drift
  check.
- 032 soft-depends on 031 (same scanner + test files, different methods) —
  execute sequentially, 031 first; expect at most trivial adjacent-hunk
  merges.

## Findings resolved independently (no plan)

- **Audit finding 6** (graph rebuilt from scratch on every command/MCP call):
  resolved by the uncommitted working-tree changes of 2026-07-16 —
  `src/Graph/GraphCache.php` adds an in-memory memo plus a fingerprinted
  on-disk cache, registered as a singleton, with a `--no-cache` escape hatch
  and `richter.cache.*` config. Covered by `tests/Feature/GraphCacheTest.php`.
- **Audit direction item D1** (markdown/PR-comment output): resolved by the
  same work — `src/Analysis/MarkdownFormatter.php` + `--markdown` on both
  commands.
- **Audit direction item D2** (persist the graph across invocations):
  resolved by the same work (the on-disk half of GraphCache).

## Findings considered and rejected

See the "Considered and rejected" sections of
[audit-2026-07-16.md](audit-2026-07-16.md) and
[audit-2026-07-19.md](audit-2026-07-19.md) for the full lists with evidence.
From the 2026-07-19 round: ImpactAnalyzer god-file extraction (defer until a
next annotation lane), unknown-static-route-name silent drops (documented
design; only the dynamic-argument escape was a bug → plan 019),
`generated_paths` traversal bypass (unreachable), `constant()` autoload
convention (established), `copy from/to` diff sections (non-default git
config only). From the 2026-07-19 consumer handoff: per-reference UNRESOLVED
taint scoping (handoff H3's "at minimum" fallback) rejected — seeds already
flow from tainted files and determinability is whole-diff, so scoping could
only weaken the exit-2 contract; see plan 032's rejection rationale. Summary
of the 2026-07-16 round, so nobody re-audits these:

- Refactoring the 4 PHPStan-baselined complexity hotspots: cosmetic; all four
  have dedicated unit tests and none blocks correctness work.
- Console-vs-MCP pipeline duplication: not present — both share the
  builder → analyzer → presenter pipeline.
- Git command/argument injection, MCP input injection, ReDoS, graph-BFS
  cycle hazards: checked, clean.
- `catch (Throwable)` sites: documented honest-degradation design, not bugs.
- Doc staleness beyond findings 3 and 16: README config table, publish tag,
  version requirements, and CI recipe verified accurate at audit time.

## Audit findings not yet planned

> **2026-07-19 update**: finding 7 became plan 017 (escalated — laravel/mcp
> 0.9.0 released). Findings 13 (still parked on upstream
> laramint/laravel-brain#65), 15 and 16 remain unplanned and were re-verified
> as still standing. Direction items DIR-3/DIR-4/DIR-5 from
> [audit-2026-07-19.md](audit-2026-07-19.md) are deliberately unplanned
> (DIR-3 gated on plan 029's corpus; DIR-4 on the Laravel-12 drop; DIR-5 is a
> small docs pass).

Original 2026-07-16 note: findings 7 (laravel/mcp version boundary), 13 (Brain contract test —
deliberately waiting for upstream laramint/laravel-brain#65 to merge),
15 (abandoned dev dependency), 16 (QA workflow docs), and
**19 (from plan-011 execution: container-binding edges silently absent
for providers opening with `declare(strict_types=1)` — upstream
laravel-brain `ContainerBindingAnalyzer` bug; Richter-side workaround
shipped in plan 012, upstream fix PR'd as laramint/laravel-brain#65)**
remain unplanned; numbering here stays monotonic as plans are added for
them. Finding 4 became plan 013; direction items D3/D4 became plans
015/016 (2026-07-18). Finding 5 is covered by plan 011 except its binding
half, which is blocked on finding 19.

- Plans 015 and 016 both add one small block to `README.md` (different
  sections: MCP server vs. benchmark) — integrate sequentially and expect
  at most a trivial adjacent-hunk merge, never a semantic conflict.
