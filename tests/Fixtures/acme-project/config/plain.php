<?php declare(strict_types=1);

use Illuminate\Support\Str;

/** Names a class, but a vendor one — a registry of app classes this is not. */
return [
    'timezone' => 'UTC',
    'helper' => Str::class,
];
